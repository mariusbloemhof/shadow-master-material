<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/buy_shadow_login"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/twitter_login_layout"
    android:gravity="left">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        android:paddingTop="10dp">

        <TextView
            android:id="@+id/txt_buy_shadow_device"
            style="@style/Shadow.Button.Text"
            android:layout_height="30dp"
            android:autoLink="all"
            android:clickable="true"
            android:drawableLeft="@drawable/ic_shopping_cart_white_64"
            android:linksClickable="true"
            android:paddingRight="5dp"
            android:text="@string/lnk_buy_shadow"
            android:textAlignment="textStart"
            android:textColor="@color/colorWhite"
            android:textColorLink="#ffffff"
            android:textSize="11dp" />

        <TextView
            android:id="@+id/txt_login_existing"
            style="@style/Shadow.Button.Text"
            android:layout_marginLeft="10dp"
            android:autoLink="all"
            android:clickable="true"
            android:drawableLeft="@drawable/ic_locked_white"
            android:linksClickable="true"
            android:paddingRight="5dp"
            android:text="@string/lnk_forgot_password"
            android:textAlignment="textStart"
            android:textColor="@color/colorWhite"
            android:textColorLink="#ffffff"
            android:textSize="11dp" />
    </LinearLayout>
</RelativeLayout>